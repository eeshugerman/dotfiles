
# TODO: implement device.only/device.not so we can replace dock-connection-handler
#   https://github.com/k0kubun/xremap/issues/201#issuecomment-1414135214
# TODO: not working with modifiers, eg Super-Shift-t, Super-a ?
# modmap:
#   - name: Swap super and left-alt
#     device:
#       only: 'AT Translated Set 2 keyboard'
#     remap:
#       ALT_L: SUPER_L
#       SUPER_L: ALT_L

keymap:
  - name: Super as modifier, a bit like MacOS's Cmd (non-terminals)
    application:
      not: ['com.github.amezin.ddterm', 'gnome-terminal-server']
    remap:
      Super-c: C-c # copy
      Super-v: C-v # paste
      Super-a: C-a # select all
      Super-t: C-t # new tab
      Super-w: C-w # close tab
      Super-z: C-z # undo

  - name: Super as modifier, a bit like MacOS's Cmd (terminals)
    application:
      only: ['com.github.amezin.ddterm', 'gnome-terminal-server']
    remap:
      Super-c: C-Shift-c # copy
      Super-v: C-Shift-v # paste
      Super-t: C-Shift-t # new tab
      Super-w: C-Shift-w # close tab

  # - name: Super-f find in Firefox (WIP)
  #   application:
  #     only: 'Firefox'
  #   remap:
  #     Super-f: [ALT-KEY_E, ALT-KEY_F]
