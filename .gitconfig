[user]
	email = eeshugerman@gmail.com
	name = Elliott Shugerman
[credential]
	helper = store
[core]
	editor = $EDITOR
[diff "ansible-vault"]
	textconv = ansible-vault view --vault-password-file=ansible/.vault_pass

[alias]
	cplog = "!git --no-pager log --oneline"
	cp = cherry-pick
